"""Live RAG evaluation harness package.

This package contains the case list, artifact schema helpers, and runner used
to exercise the real RAG + router stack against an LLM and write structured
JSON artifacts under ``test-artifacts/rag-eval/<run_id>/``.
"""
